<!--pages/main-video/main-video.wxml-->
<view class="main-video">

  <block wx:for="{{ videoList }}" wx:key="id">
    <video-item 
      class="item" 
      videoItem="{{ item }}"
      bindtap="enterDetailVideoPage"
      data-vid="{{ item.id }}"
    ></video-item>
  </block>
  
</view>
