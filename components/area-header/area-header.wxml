<!--components/area-header/area-header.wxml-->
<view class="area-header">
  <view class="left">{{ title }}</view>
  <view 
    wx:if="{{ hasMore }}" 
    class="right"
    bindtap="onDetailMenuClick"
  >
    <text>更多</text>
    <van-icon name="arrow" />
  </view>
</view>
